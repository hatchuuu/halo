<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 p-8">
    <h1 class="text-3xl font-bold mb-8"><%= title %></h1>
    
    <table class="min-w-full bg-white">
        <thead>
            <tr>
                <th class="py-2">ID</th>
                <th class="py-2">Nama</th>
                <th class="py-2">NIP</th>
                <th class="py-2">Mata Kuliah</th>
            </tr>
        </thead>
        <tbody>
            <% datas.forEach(dosen => { %>
                <tr>
                    <td class="border px-4 py-2"><%= dosen.id %></td>
                    <td class="border px-4 py-2"><%= dosen.nama %></td>
                    <td class="border px-4 py-2"><%= dosen.nip %></td>
                    <td class="border px-4 py-2"><%= dosen.matkul %></td>
                </tr>
            <% }) %>
        </tbody>
    </table>

    <a href="/logout" class="text-blue-500 mt-8 inline-block">Logout</a>
</body>
</html>
